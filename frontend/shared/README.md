# Shared Frontend Contract

Role portals may reuse generic utilities from `frontend/js/shared/` and generic styles from `frontend/css/shared/`.

Portal-specific routes, sidebars, dashboard composition, and API namespaces stay inside `frontend/pages/{role}/`, `frontend/js/{role}/`, and `frontend/css/{role}/`.
